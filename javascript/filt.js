function getValue(arr) {
    let obj = [];

    obj.push({"a":0});
    obj.push({"b":1});
    console.log(JSON.stringify(obj));
    // const keys = Object.keys(arr);
    // const map = new Map();

    // for(let i = 0; i < keys.length; i++) {
    //     map.set(keys[i], arr[keys[i]]);
    // }
    // console.log('map is ======= ',map);

    // let arre = [1,2,3,'Himanshu'];
 
}

let res = getValue([1,2,3,4,'Himanshu']);
console.log(res);